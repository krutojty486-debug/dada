{
  "version": 1,
  "scripts": {
    "setup": "echo 'no dependencies: single-file static page'",
    "run": "python3 -m http.server ${PORT:-3000} --bind 0.0.0.0"
  },
  "ports": {
    "default": 3000
  }
}
